/**
 * `brief/` — ~3-field capture via QuickInput; persistence. Optional; feeds
 * context into `coaching` (Component Overview, ADR-007).
 *
 * Populated by task 14 (brief capture + persistence).
 */
export {};
