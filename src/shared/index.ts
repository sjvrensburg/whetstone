/**
 * `shared/` — Types, config, SecretStorage access, canonical JSON,
 * hashing/signing. No dependencies on other modules (Component Overview).
 *
 * Populated by task 02 (domain types + coaching schema), task 03 (canonical
 * JSON, SHA-256, Ed25519), and task 04 (config + SecretStorage access).
 */
export {};
