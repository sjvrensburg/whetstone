/**
 * `guard/` — `RefusalGuard.screen()`: deterministic checks + cloud judge. A pure
 * boundary with no UI, red-team-tested in isolation (Component Overview,
 * ADR-003). The brand-defining voice-preservation invariant lives here.
 *
 * Populated by task 10 (deterministic layer + `screen()`) and task 11 (judge).
 */
export {};
