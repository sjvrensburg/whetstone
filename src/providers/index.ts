/**
 * `providers/` — `CoachingProvider` interface + Anthropic reference impl
 * (OpenAI etc. later). The only place that talks to a cloud endpoint; reads the
 * BYO key from SecretStorage (Component Overview, ADR-004).
 *
 * Populated by task 09 (interface + Anthropic `coach`/`judge` + registry).
 */
export {};
