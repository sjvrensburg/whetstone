/**
 * `ledger/` — Append-only hash chain, Ed25519 checkpoints, `verify()`,
 * `report()`, `exportDisclosure()`, external-insert detector. Single append
 * chokepoint; read-side report/export (Component Overview, ADR-006).
 *
 * Populated by task 07 (hash chain + checkpoints + verify), task 08 (external-
 * insert detector), and task 16 (transparency report + ICMJE disclosure).
 */
export {};
