/**
 * `consent/` — Just-in-time cloud-send consent + key setup; records the send.
 * The gate in front of the first egress; writes `cloud_send` events (Component
 * Overview, F7).
 *
 * Populated by task 13 (consent gate + key setup).
 */
export {};
