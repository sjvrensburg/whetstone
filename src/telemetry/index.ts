/**
 * `telemetry/` — Opt-out instrumentation (self-return, helpfulness,
 * voice-preservation sampling). Redacts prose/keys; local aggregation
 * (Component Overview).
 *
 * Populated by task 18 (opt-out telemetry instrumentation).
 */
export {};
