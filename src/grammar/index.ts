/**
 * `grammar/` — harper.js (WASM) linting, LaTeX masking, lint->diagnostic
 * mapping, "explain rule". Local-only; routes explanations through `providers`
 * (never rewrites) (Component Overview, ADR-005).
 *
 * Populated by task 05 (lint + masking + diagnostics), task 06 (hover/dismiss),
 * and task 15 (explain-this-rule action).
 */
export {};
